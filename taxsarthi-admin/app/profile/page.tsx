import { UpdateProfile } from "@/components/UpdateProfile";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <UpdateProfile />
  );
};

export default page;
